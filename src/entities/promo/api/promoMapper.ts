import type { Promo, PromoCheck, PromoEvaluation } from '@/entities/promo/model/types';
export const mapPromo = (value: any): Promo => ({ id: Number(value.id), name: String(value.name ?? ''), text: String(value.text ?? ''), conditionText: String(value.condition_text ?? ''), cityId: Number(value.city_id ?? 0), dateFrom: String(value.date_from ?? ''), dateTo: String(value.date_to ?? '') });
export const mapCheck = (value: any): PromoCheck => ({ valid: Boolean(value.valid), promo: value.promo ? { id: Number(value.promo.id), name: String(value.promo.name ?? ''), text: String(value.promo.text ?? ''), conditionText: String(value.promo.condition_text ?? '') } : null });
export const mapEvaluation = (value: any): PromoEvaluation => ({ valid: Boolean(value.valid), code: String(value.code ?? ''), promo: mapPromo(value.promo), subtotal: Number(value.subtotal ?? 0), discount: Number(value.discount ?? 0), total: Number(value.total ?? 0), freeDrive: Boolean(value.free_drive), items: Array.isArray(value.items) ? value.items : [], effect: value.effect ?? null, writes: Boolean(value.writes) });
